# Credentials. Never commit these. A leaked key in a git history is not fixable
# by deleting the file later, because the history keeps it.
.env
.env.local
*.key
*-service-account*.json
serviceAccountKey.json

# Dependencies and generated output
node_modules/
dist/
.lighthouseci/

# Editor and operating system
.vscode/
.DS_Store
Thumbs.db
desktop.ini
